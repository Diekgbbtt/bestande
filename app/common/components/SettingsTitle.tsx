import React from 'react';
import {
	BlockTextProps,
	BlockTextTitle,
} from '../../../core/components/BlockTextTitle';

export const SettingsTitle = (props: BlockTextProps) => (
	<BlockTextTitle style={{marginBottom: 5}} {...props} />
);
