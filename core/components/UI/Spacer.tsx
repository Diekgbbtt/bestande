import {View} from 'react-native';
import styled from 'styled-components/native';
import {BASE_PADDING} from './Base';

export const Spacer = styled(View)`
	height: ${BASE_PADDING}px;
	width: ${BASE_PADDING}px;
`;
